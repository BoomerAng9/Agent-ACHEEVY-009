# ===========================================================================
# A.I.M.S. Environment Configuration
# Copy this file to .env and fill in the values
# ===========================================================================

# UEF Gateway
PORT=3001
LOG_LEVEL=info
NODE_ENV=production

# CORS (comma-separated origins, or * for all)
CORS_ORIGIN=http://localhost:3000

# Agent Endpoints (Layer 3 — enable when agents are deployed)
# AGENT_ZERO_URL=http://agent-zero:8080
# AGENT_BRIDGE_URL=http://agent-bridge:3010

# Firebase (Layer 4 — enable when persistence is configured)
# FIREBASE_PROJECT_ID=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_PRIVATE_KEY=

# LLM API Keys (for agents)
# LLM_API_KEY=
